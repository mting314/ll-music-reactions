# Local dev (`bun run dev`) fetches data from the same Cloud Run data API as
# production, since there is no bundled dataset anymore. Override in
# .env.development.local (gitignored) to point at a local data API.
VITE_DATA_API=https://ll-data-api-278890546984.us-central1.run.app
