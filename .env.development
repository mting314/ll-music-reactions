# Local dev (`bun run dev`) fetches data from the same URL as production. Override
# in .env.development.local (gitignored) to point elsewhere. See VITE_DATA_URL in
# .env.production for the cutover plan to static Firebase Hosting.
VITE_DATA_URL=https://ll-data-api-278890546984.us-central1.run.app/data
